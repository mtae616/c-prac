#include <stdio.h>

int main() 
{
  int a, A; // a와 A는 다르다!
  // int 1hi; // 숫자가 앞에 위치할 수 없다!
  int hi123, h123i, h1234324; // 숫자가 뒤에 위치하면 괜찮다
  int 한글; // 한글 변수 가능
  int space bar // 변수 중간에 띄어쓰기 안됨 underscorecase
  int space_bar;
  int enum, long, double, int; // 예약어 안돼!

  return 0;
}